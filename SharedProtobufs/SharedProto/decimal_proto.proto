syntax = "proto3";

//Sharable Decimal potobuf 

message DecimalProto {

    // Whole units part of the amount
    int64 units = 1;

    // Nano units of the amount (10^-9)
    // Must be same sign as units
    sfixed32 nanos = 2;
}